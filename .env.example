########################################
# 数据库配置（必须替换为你的实际信息）
# DB_HOST: 容器访问宿主机MySQL的地址；Linux推荐直接写宿主机IP，或保留 host.docker.internal（Compose 已配置 host-gateway）
DB_HOST=host.docker.internal
# DB_NAME: 目标数据库名，需预先在 MySQL 中创建
DB_NAME=wxnotion
# DB_USERNAME/DB_PASSWORD: MySQL 账号与密码
DB_USERNAME=root
DB_PASSWORD=root

########################################
# 微信公众平台配置（必须替换为你的服务号配置）
# WX_APP_ID: 公众号 AppID（在公众平台后台查看）
WX_APP_ID=wx_your_app_id
# WX_APP_SECRET: 公众号 AppSecret（在公众平台后台查看）
WX_APP_SECRET=your_app_secret
# WX_TOKEN: 服务器配置中的 Token，用于签名校验；与后台保持一致
WX_TOKEN=your_token
# WX_ENCODING_AES_KEY: 消息加解密密钥（兼容/安全模式需要），长度43字符，来自后台服务器配置页面
WX_ENCODING_AES_KEY=your_encoding_aes_key_43chars

########################################
# 应用内部加密配置
# AES_KEY: 16或32字节的十六进制字符串（例如32个hex字符=16字节；64个hex字符=32字节），用于加密存储Notion API Key
AES_KEY=0123456789abcdef0123456789abcdef

########################################
# Notion API版本（建议保持默认）
NOTION_VERSION=2022-06-28

########################################
# Spring Profile（生产环境）
SPRING_PROFILES_ACTIVE=prod

# 说明：
# 1) 将本文件复制为 .env 后再修改为你的实际值：
#    cp .env.example .env && nano .env
# 2) Docker Compose 会自动读取 .env 并注入容器环境变量；Spring Boot 会在运行时使用这些变量覆盖 application-prod.yml 中的默认配置。
# 3) 请勿将包含真实密钥的 .env 文件提交到版本库；仅提交 .env.example 供参考。
