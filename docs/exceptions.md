常见异常与处理

- API调用超时：返回“同步异常”，提示稍后重试
- 密钥失效：验证失败或创建失败，提示检查API Key或重新配置
- 数据库不存在/无title属性：创建抛错并反馈；建议用户检查数据库ID与属性设置
- 微信签名不通过：拒绝请求，返回空响应

排查要点

- 检查 `WX_APP_ID/SECRET/TOKEN` 与服务器公网地址是否一致
- 检查 `AES_KEY` 是否为16或32字节长度的hex串
- 检查 Notion 数据库的 title 属性存在

